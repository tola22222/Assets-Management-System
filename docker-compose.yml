services:
  app:
    image: toladev1/asset-management:latest
    container_name: asset_management_app
    restart: always
    working_dir: /var/www
    env_file:
      - .env
    volumes:
      - ./.env:/var/www/.env
      - ./:/var/www # Mounts current dir to share files with Nginx
    depends_on:
      - db
    networks:
      - laravel_net

  web:
    image: nginx:alpine
    container_name: asset_management_web
    restart: always
    ports:
      - "80:80"
    volumes:
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf
      - ./:/var/www # Nginx needs access to /var/www/public
    depends_on:
      - app
    networks:
      - laravel_net

  db:
    image: mysql:8.0 # Switched to MySQL
    container_name: asset_management_db
    restart: always
    command: --default-authentication-plugin=mysql_native_password
    environment:
      MYSQL_DATABASE: asset_management_db
      MYSQL_USER: asset_management_user
      MYSQL_PASSWORD: asset_management_pass
      MYSQL_ROOT_PASSWORD: root_pass
    volumes:
      - dbdata:/var/lib/mysql
    networks:
      - laravel_net

  phpmyadmin:
    image: phpmyadmin
    container_name: asset_management_phpmyadmin
    restart: always
    ports:
      - "8080:80"
    environment:
      PMA_HOST: db
    depends_on:
      - db
    networks:
      - laravel_net

networks:
  laravel_net:

volumes:
  dbdata: